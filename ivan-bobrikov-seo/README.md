# משימת SEO וביצועים — ivan-bobrikov.com — דוח מצב

תאריך: 2026-07-20

## סטטוס: העבודה באתר עצמו לא בוצעה — חסימת גישה מהסביבה הזאת

הסשן הזה רץ בסביבת ענן מרוחקת ומבודדת (Claude Code on the web), לא במחשב המקומי:

1. **אין גישה לדפדפן המחובר** — הדפדפן הפתוח והמחובר ל-wp-admin נמצא במחשב המקומי.
   לסשן ענן אין גישה אליו ואין פרטי התחברות לוורדפרס.
2. **מדיניות הרשת של הסביבה חוסמת את הדומיין** — כל בקשה ל-ivan-bobrikov.com
   (כולל הדף הציבורי) נחסמת עם 403 ברמת ה-proxy. גם ביקורת קריאה-בלבד לא אפשרית.

## מה כן הוכן (בתיקייה הזאת)

- `faq-draft.md` — טיוטת 8 שאלות ותשובות לשלב 6, לאישור. עובדות לא ידועות סומנו [להשלמה].
- `local-business-schema.jsonld` — תבנית JSON-LD מלאה מסוג HealthAndBeautyBusiness לשלב 1,
  עם כל השדות שנדרשו; שדות חסרים סומנו [להשלמה].
- `snippets-and-alt-texts.md` — קטעי קוד מוכנים: הטמעת Schema, שנה דינמית בפוטר,
  loading/width/height/fetchpriority לתמונות, 19 וריאציות טקסט ALT לתמונות Polish_*,
  ו-aria-label לאייקונים.

## מה נדרש כדי להמשיך

1. **להריץ את המשימה מהסביבה הנכונה**: סשן Claude במחשב עם גישה לדפדפן המחובר
   (אפליקציית Desktop / Claude in Chrome) — שם אפשר לעבוד ישירות ב-wp-admin.
   לחלופין: לאפשר את ivan-bobrikov.com במדיניות הרשת של סביבת הענן וליצור
   WordPress Application Password — ואז אפשר לעבוד דרך ה-REST API (עמודים, מדיה, ALT).
2. **להשלים את הפרטים החסרים במשימה**: כתובת מלאה, שעות פעילות,
   ואיזה פרופיל אינסטגרם הוא הנכון (i.will.b_fit או ivan.v.bobrikov).
3. **לאשר את טיוטת השאלות והתשובות** ולהשלים את הסעיפים המסומנים [להשלמה].
